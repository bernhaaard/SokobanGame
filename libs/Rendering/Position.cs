namespace libs;

public struct Position {
    public int X { get; }
    public int Y { get; }

    // Constructor to set X and Y coordinates
    public Position(int x, int y) {
        X = x;
        Y = y;
    }
}